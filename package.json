{
  "name": "cartcrft-workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "suite": "pnpm --filter backend suite",
    "migrate": "pnpm --filter backend migrate"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
