{
  "name": "@cartcrft/storefront",
  "version": "0.1.0",
  "private": true,
  "description": "Cartcrft drop-in storefront script — cart store, native checkout, auth client",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3",
    "@types/node": "^22.15.30",
    "vitest": "^3.2.3"
  }
}
