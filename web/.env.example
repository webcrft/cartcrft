# Base URL of the Cartcrft backend API the dashboard SPA talks to.
# Astro only exposes vars prefixed with PUBLIC_ to client-side code.
# Falls back to http://localhost:8080 when unset (see src/dashboard/lib/sdk.ts).
PUBLIC_API_URL=http://localhost:8080
